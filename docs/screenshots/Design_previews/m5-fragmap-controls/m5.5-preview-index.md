# M5.5 Preview Index - Bulk Actions

## M5.5 Scope Lock
- `M5.5` only.
- Includes bulk actions only:
  - `Hide all`
  - `Reset defaults`
- Excludes `M5.6+` behavior:
  - no reliability hardening beyond current row-level contracts,
  - no new map/protein/ligand interaction semantics outside bulk actions.

## Coverage Checklist
- [x] `Default` state: all bulk action buttons available in the FragMap panel.
- [x] `Loading` state: in-progress bulk action disables both in-panel bulk-action buttons until completion.
- [x] `Empty` state: `Hide all` result leaves zero visible maps while keeping cached map assets.
- [x] `Error` state: `Reset defaults` retry path shows partial failure isolation without app-level failure.
- [x] `Success` state: bulk actions complete in place (`Reset defaults` restores default visibility/iso values).
- [x] Scope constrained to bulk actions only (`M5.5`).

## Artifact Matrix
| State | Artifact | Notes |
|---|---|---|
| Default | `desktop/m5.5-bulk-actions-states.svg` (Panel A) | Bulk action row active with non-default working state |
| Loading | `desktop/m5.5-bulk-actions-states.svg` (Panel B) | Action-in-progress lockout for `Hide all` |
| Empty | `desktop/m5.5-bulk-actions-states.svg` (Panel C) | Zero visible maps after `Hide all`; cached handles retained |
| Error | `desktop/m5.5-bulk-actions-states.svg` (Panel D) | `Reset defaults` partial retry failure keeps failed row disabled |
| Success | `desktop/m5.5-bulk-actions-states.svg` (Panel E) | `Reset defaults` success outcome with camera unchanged |

## Open UI Questions
1. None currently; this preview follows the existing M5 action-row style and behavior constraints.

## Traceability
- Plan refs: `docs/plans/execution-plan.md` Sections `1.3`, `3`, and `3.1` (`M5.5`).
- Spec refs: `docs/specs/fragmap-controls-spec.md` Sections `8.3`, `9`, and `13`.

## Approval State
- Current: `PASS` (`APPROVED UI PREVIEW` received for Prompt A; Prompt B implementation completed).
