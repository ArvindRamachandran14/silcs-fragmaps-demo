# M5.2b Preview Index - Protein Visibility Toggle

## Scope
- Slice: `M5.2b Protein Visibility Toggle`.
- Prompt type: `Prompt A` (design preview only).
- Prerequisite: `M5.2a` design and implementation gates closed.
- Runtime implementation is out of scope until explicit `APPROVED UI PREVIEW`.

## Scope Lock
- `M5.2b` adds one behavior only: in-place `Protein cartoon` show/hide in the `FragMap` tab.
- Default state for this toggle is `ON`.
- Excludes `M5.3+` behavior:
  - Advanced/Exclusion behavior changes,
  - per-map iso controls,
  - bulk actions behavior changes,
  - reliability hardening and retry UX finalization.

## Required State Coverage
- Default
- Loading
- Empty
- Error
- Success

## Planned Artifacts
- `desktop/m5.2b-protein-visibility-states.svg` (pending)

## Open UI Questions
- None currently.

## Approval State
- `BLOCKED-DESIGN` (awaiting explicit `APPROVED UI PREVIEW` for `M5.2b`)
