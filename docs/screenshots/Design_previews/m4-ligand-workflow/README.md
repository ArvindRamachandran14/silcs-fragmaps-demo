# M4 Ligand Workflow Design Preview Packet

This packet is the first-pass skeleton for the M4 Design Preview Gate.

## Gate Status
- Milestone: `M4`
- Gate state: `PENDING`
- Required approval token: `APPROVED UI PREVIEW`

## Packet Structure
- `preview-index.md`: artifact checklist and acceptance traceability.
- `approval-log.md`: approval record and reviewer comments.
- `desktop/`: desktop preview artifacts.
- `mobile/`: mobile preview artifacts (if responsive behavior changes).
- `states/`: per-state guidance and placeholder entries.

## Required States
- Default
- Loading
- Empty (both-unchecked)
- Error (pose load failure)
- Success (in-place switch/pose update)

## Notes
- This location is intentionally under `docs/screenshots/Design_previews` per current user instruction.
- Include final artifact links in `docs/plans/milestone-inventory.md` when gate is completed.
