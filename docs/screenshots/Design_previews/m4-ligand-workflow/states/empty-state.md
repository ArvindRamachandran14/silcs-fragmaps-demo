# Empty State (Both-Unchecked) Guidance (M4A)

Expected content:
- Persistent empty-state panel in ligand section.
- Message explains no poses are visible for current ligand.
- Recovery actions visible: `Show Baseline`, `Show Refined`, `Show Both`.

Artifacts:
- `../desktop/empty-state-both-unchecked.svg`
