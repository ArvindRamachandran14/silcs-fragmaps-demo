# Empty State (Both-Unchecked) Guidance

Expected content:
- Persistent empty-state panel in ligand section.
- Message explains no poses are visible for current ligand.
- Recovery actions visible: `Show Baseline`, `Show Refined`, `Show Both`.

Attach artifacts:
- `../desktop/empty-state-both-unchecked.png`
- `../mobile/empty-state-both-unchecked.png` (if mobile variant differs)
