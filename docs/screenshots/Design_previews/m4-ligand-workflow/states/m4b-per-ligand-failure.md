# M4B Per-Ligand Failure State Guidance

Expected content:
- Per-ligand/pose failure surfaces as non-blocking context.
- Only affected pose control is disabled.
- Unaffected controls and viewer interactions remain available.

Artifact:
- `../desktop/m4b-per-ligand-failure.svg`
